import { createApp } from 'vue'
import { recursiveFind } from '@suger-tdy/utils/collection'
import App from './App.vue'

// eslint-disable-next-line no-console
console.log(recursiveFind)
createApp(App).mount('#app')
